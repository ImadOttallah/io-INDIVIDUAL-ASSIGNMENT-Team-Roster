// import { getSinglePlayer } from "./playerData"

// const viewPlayerDetails = (playerFireBaseKey) => new Promise((resolve, reject) => {
//   Promise.all([getSinglePlayer(playerFireBaseKey)]).then((playerObject) => {
//   })
// }
